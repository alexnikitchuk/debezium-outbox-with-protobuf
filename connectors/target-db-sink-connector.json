{
  "name": "db-sink-connector",
  "config": {
    "connector.class": "io.confluent.connect.jdbc.JdbcSinkConnector",
    "tasks.max": "1",
    "topics": "customers_topic",
    "connection.url": "jdbc:postgresql://target-db:5432/postgres?user=postgres&password=postgres",
    "auto.create": "true"
  }
}
